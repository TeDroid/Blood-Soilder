<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mkrlabs.bloodsoilder">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BloodSoilder">
        <activity android:name=".ui.account.userinformation.UserAccountInformation">
        </activity>
        <activity android:name=".ui.account.userinformation.AddProfilePictureActivity"></activity>
        <activity android:name=".ui.account.userinformation.DonorInfoActivity"></activity>
        <activity android:name=".ui.account.userinformation.NidInfoActivity"></activity>
        <activity android:name=".ui.account.userinformation.PersonalInfoActivity" />
        <activity android:name=".ui.account.VerifyAccountActivity" />
        <activity android:name=".ui.account.CreateAccountActivity" >

        </activity>
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/MAPS_API_KEY" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>